// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id         Int      @id @default(autoincrement())
  employeeId String   @unique
  name       String   @db.VarChar(45)
  email      String   @unique
  password   String   @db.VarChar(60)
  createdAt  DateTime @default(now()) @map(name: "created_at")
  updatedAt  DateTime @updatedAt @map(name: "updated_at")
  roleId     Int

  role Role @relation(fields: [roleId], references: [id])
}

model Role {
  id       Int    @id @default(autoincrement())
  roleName String @db.VarChar(45)
  userId   Int

  user User[]
}

model FormGirdisiExample {
  id  Int @id @default(autoincrement())
  category String @db.VarChar(45)
  dateAndTime DateTime  @default(now()) @map(name: "date_and_time")
  createdAt DateTime @default(now()) @map(name: "created_at")
  updatedAt DateTime @updatedAt @map(name: "updated_at")
  createdById Int 
  updatedById Int
}


//SU 
model IsletmeSuyuKontrolu{
  id   Int   @id @default(autoincrement())
  ph String @db.VarChar(45)
  sertlik String @db.VarChar(45)
  bikarbonat String @db.VarChar(45)
}
model IcmeSuyuTesisiKontrolFormu{
  id   Int   @id @default(autoincrement())
  hamsusayac String @db.VarChar(45)
  hamsuTonGun String @db.VarChar(45)
  uretilenSuTonGun String @db.VarChar(45)
  klorCozHazir String @db.VarChar(45)
  klorAnalizSonucuMgL String @db.VarChar(45)
  genelTemizlik String @db.VarChar(45)
  aciklama String @db.VarChar(45)
}
model YemekhaneVeKullanmaSuyu{
  id   Int   @id @default(autoincrement())
  klorCozeltisiDozaji String @db.VarChar(45)
  klor String @db.VarChar(45)
  ph String @db.VarChar(45)
  iletkenlik String @db.VarChar(45)
  genelTemizlik String @db.VarChar(45)
  aciklama String @db.VarChar(45)
}


